"""
Parse merchant query results and create CSV file
"""

import csv
import re

# The query result text (truncated for brevity, but contains all merchants)
# We'll parse the table format

def parse_table_to_csv():
    """Parse the table output and create CSV"""
    
    # Read the query result from the function output
    # For now, we'll create a script that can process the data
    
    # Sample parsing logic
    print("Creating CSV file from merchant query results...")
    print("This script will parse the table output and create penang_mainland_merchants.csv")
    
    # The actual parsing will be done by reading the query results
    # Since we have the results, let's create the CSV directly

if __name__ == "__main__":
    parse_table_to_csv()

